# Break/Fix — Import Failure

Use an incorrect subscription ID or Resource Group ID during `terraform import`.

Work through:
**Symptom → Evidence → Diagnosis → Root Cause → Fix → Validation → Prevention**

Do not simply retry with a different command. Explain which identity/resource scope was incorrect and why the corrected import address works.
