# Validation Standards

Validators are learner-facing proof mechanisms. They return explicit `[PASS]` / `[FAIL]`, identify the failed control and suggest the next diagnostic action without revealing the reference solution.
